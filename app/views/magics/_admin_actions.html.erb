<% if logged_in?(:site_admin) %>
  <nav class="breadcrumb blogcrumb">
    <%= link_to fa_icon('file-text'), toggle_status_magic_path(magic), style: blog_status_color(magic) %>
    <%= link_to fa_icon('pencil-square-o'), edit_magic_path(magic) %>
    <%= link_to fa_icon('trash'), magic, method: :delete, data: { confirm: 'Are you sure?' } %>
  </nav>
<% end %>